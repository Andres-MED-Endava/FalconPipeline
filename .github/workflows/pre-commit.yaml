name: pre-commit

on:
   pull_request
push:
        branches: [main]

jobs:
        pre-commit:
                steps:
                        - uses: actions/checkout@v2
                        - uses: actions/setup-python@v2
                        - uses: pre-commit/actions@1.0.3
